import React from "react";

const DemoBanner = () => {
  return (
    <div className="demo-banner">
      <p>You’re exploring DelphiaFit in Demo Mode — all actions are read‑only.</p>
    </div>
  );
};

export default DemoBanner;
